/** @type {import('next-sitemap').IConfig} */
module.exports = {
  siteUrl:  'https://nyontech.vercel.app' || 'https://nyon.in', // Your website URL
  generateRobotsTxt: true, // Optionally generate a `robots.txt` file
};